import React, { FC } from 'react';


interface Props {}


export const SelectionLayer: FC<Props> = (props: Props) => {

  return <div className="SelectionLayer" {...props}/>;
};

export default SelectionLayer;
